# Desktop Shortcuts

Plantillas `.desktop` para tareas administrativas comunes del entorno
`OpenClaw`.

Se instalan con:

```bash
scripts/desktop/install-shortcuts.sh apply
```

El script expande `__REPO_ROOT__` y escribe los accesos directos en:

- `~/.local/share/applications`
